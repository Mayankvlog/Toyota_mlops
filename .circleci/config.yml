version: 2.1

jobs:
  build:
    docker:
      - image: python:3.8

    steps:
      - checkout

      # Install dependencies
      - run:
          name: Install Dependencies
          command: |
            pip install -r requirements.txt

      # Run pytest and store results
      - run:
          name: Run Tests
          command: |
            pytest -v --junitxml=test-results/junit.xml
      - store_test_results:
          path: test-results

      # Save plots as artifacts
      - persist_to_workspace:
          root: .
          paths:
            - scatter_plot.html
            - pie_plot.html
            - bar_plot.html
            - line_plot.html

  deploy:
    docker:
      - image: python:3.8

    steps:
      - add_ssh_keys:
          fingerprints:
            #- "SHA256:your_ssh_key_fingerprint"
            - "SHA256:Msw+ELHRFCtXJUO0ONwxfTcJOsqDCfKfCIzfkBH9RLI"
            
      - run:
          name: Deploy Over SSH
          command: |
            #ssh $SSH_USER@$SSH_HOST "<remote deploy command>"
            #ssh $SSH_USER@$git.circleci.com "<remote deploy commands>"

workflows:
  version: 2
  build_and_deploy:
    jobs:
      - build:
          filters:
            branches:
              only:
                - master
      - deploy:
          requires:
            - build
          filters:
            branches:
              only:
                - master
